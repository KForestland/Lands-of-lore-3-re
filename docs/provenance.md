# Provenance

Current public LoL3 claims in this repo come from:

- local extraction and section descent work on LoL3 `LMF` / `WSX` / opaque blob families
- local Wine runtime tracing on the Linux install
- local save-witness comparisons
- repeated static multi-window comparison passes
- repeated falsification and replication passes on:
  - `QWOD0`
  - `SWOD4`
  - `CKEEP`

Current public checkpoint was promoted only after claims were pressure-tested across:

- multiple body windows where possible
- competing structural readings
- hostile or narrowing review passes
- public-safe compression into stricter repo wording

Practical source types behind the promoted docs:

- local analysis outputs in the private working tree
- promoted checkpoint memos in `AI_COMMS`
- public diagrams rebuilt from those promoted checkpoint results

Not treated as canonical by themselves:

- external advisory model suggestions
- pretty but weak naming guesses
- unreplicated byte-pattern stories
- one-off scratch scripts without replication
- broad semantic invention above the byte-behavior layer
