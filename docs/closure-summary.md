# Closure Summary

The LoL3 lane is no longer blocked on setup or broad structure.

What is effectively closed:

- `LMF` container table structure
- recursive local-header descent for the main opaque blob stack
- runtime provenance:
  - `QWOD0` and `SWOD4` are quiet baseline families
  - `QWOD0S` is a higher-activity sidecar
- shared outer typed-region / regime-path model
- byte-level semantic checkpoint on the two true semantic anchors:
  - `QWOD0`
  - `SWOD4`

What is not fully closed:

- higher in-engine meaning above the byte-behavior level
- full unification of all witness families at the semantic layer
- patch/rebuild implications

Strongest current semantic spine:

- `QWOD0`
  - `ad -> c5 -> a5`
  - `eb` as mixed self-holding side member
  - strongest durable settled edge:
    - `c5 -> a5`
- `SWOD4`
  - early `0b -> 02 -> 06`
  - later `0b -> 00`
  - `00 -> 00`
  - strongest coarse takeover span:
    - `00` dominance across `0768-1280`

Practical read:

- the remaining uncertainty is mostly meaning refinement, not structural uncertainty
- the safest repo-grade anchors are:
  - `QWOD0`: `c5 -> a5`
  - `SWOD4`: `0b -> 00`, `00 -> 00`
